version: 2
models:
- name: im_fact_device_telemetry
  data_tests:
  - dbt_constraints.primary_key:
      arguments:
        column_names:
        - PAIRED_DEVICE_BK
        - DEVICE_BK
        - AGGREGATE_DATE
  columns: []
