version: 2

models:
  - name: dim_date
    description: Common dimension dates, date parts and fiscal calendars
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - date_day