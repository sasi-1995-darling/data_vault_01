version: 2

models:
  - name: dim_invoice
    description: Sales dimension for sales invoices
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - dim_invoice_pk
    config:
      tags:
        - emtek