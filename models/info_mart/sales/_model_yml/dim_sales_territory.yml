version: 2

models:
  - name: dim_sales_territory
    description: Sales dimension for sales territory
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - dim_sales_territory_pk
    config:
      tags:
        - emtek