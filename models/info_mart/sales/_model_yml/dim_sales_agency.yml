version: 2

models:
  - name: dim_sales_agency
    description: Sales dimension for sales agency
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - dim_sales_agency_pk
    config:
      tags:
        - emtek