version: 2

models:
  - name: dim_customer_ship_location
    description: Sales dimension for customer ship-to locations
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - dim_customer_ship_location_pk
    config:
      tags:
        - emtek