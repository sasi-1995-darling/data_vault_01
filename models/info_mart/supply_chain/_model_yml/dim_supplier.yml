version: 2

models:
  - name: dim_supplier
    description: Supply chain dimension for suppliers
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - dim_supplier_pk
    config:
      tags:
        - emtek