version: 2
models:
  - name: v_psa_stg_customer_site_use__ml_ebs
    tests:
      - dbt_utils.unique_combination_of_columns:
          config:
            severity: warn
          arguments:
            combination_of_columns:
              - SITE_USE_ID
              - LOAD_DTS
    columns:
      - name: CUSTOMER_BK
        tests:
          - not_null:
              config:
                severity: warn