version: 2
models:
  - name: v_psa_stg_receivables__ml_ebs
    tests:
      - dbt_utils.unique_combination_of_columns:
          config:
            severity: warn
          arguments:
            combination_of_columns:
              - ORG_ID
              - RECEIVABLES_TRX_ID
              - LOAD_DTS
    columns:
      - name: RECEIVABLES_BK
        tests:
          - not_null:
              config:
                severity: warn
      - name: LOAD_DTS
        tests:
          - not_null:
              config:
                severity: warn