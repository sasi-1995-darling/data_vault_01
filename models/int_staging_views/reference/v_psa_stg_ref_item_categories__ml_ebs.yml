version: 2
models:
  - name: v_psa_stg_ref_item_categories__ml_ebs
    tests:
      - dbt_utils.unique_combination_of_columns:
          config:
            severity: warn
          arguments:
            combination_of_columns:
              - INVENTORY_ITEM_ID
              - CATEGORY_SET_ID
              - CATEGORY_ID
              - LOAD_DTS
    columns: []