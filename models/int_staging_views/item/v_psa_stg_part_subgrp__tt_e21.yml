version: 2
models:
  - name: v_psa_stg_part_subgrp__tt_e21
    tests:
      - dbt_utils.unique_combination_of_columns:
          config:
            severity: warn
          arguments:
            combination_of_columns:
              - PART_SUBGRP_ID
              - LOAD_DTS
    columns:
      - name: PART_SUBGRP_ID
        tests:
          - not_null:
              config:
                severity: warn
      - name: LOAD_DTS
        tests:
          - not_null:
              config:
                severity: warn