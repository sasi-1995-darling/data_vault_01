version: 2
models:
  - name: v_psa_stg_notification_types
    tests:
      - dbt_utils.unique_combination_of_columns:
          config:
            severity: warn
          arguments:
            combination_of_columns:
              - QMART
              - LOAD_DTS
    columns:
      - name: QMART
        tests:
          - not_null:
              config:
                severity: warn