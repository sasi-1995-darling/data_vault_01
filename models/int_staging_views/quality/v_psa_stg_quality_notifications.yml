version: 2
models:

  - name: v_psa_stg_quality_notifications
    tests:
      - dbt_utils.unique_combination_of_columns:
          config:
            severity: warn
          arguments:
            combination_of_columns:
              - QMNUM
              - LOAD_DTS
    columns:
      - name: QUALITY_NOTIFICATION_BK
        tests:
          - not_null:
              config:
                severity: warn
      - name: QMNUM
        tests:
          - not_null:
              config:
                severity: warn