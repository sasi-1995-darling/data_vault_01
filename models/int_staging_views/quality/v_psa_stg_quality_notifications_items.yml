version: 2
models:
  - name: v_psa_stg_quality_notifications_items
    tests:
      - dbt_utils.unique_combination_of_columns:
          config:
            severity: warn
          arguments:
            combination_of_columns:
              - QUALITY_ISSUE_HK
              - LOAD_DTS
    columns:
      - name: QUALITY_ISSUE_BK
        tests:
          - not_null:
              config:
                severity: warn
      - name: QMNUM
        tests:
          - not_null:
              config:
                severity: warn
      - name: FENUM
        tests:
          - not_null:
              config:
                severity: warn