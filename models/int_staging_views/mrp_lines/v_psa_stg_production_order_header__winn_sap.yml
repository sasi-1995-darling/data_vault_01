version: 2
models:
  - name: v_psa_stg_production_order_header__winn_sap
    tests:
      - dbt_utils.unique_combination_of_columns:
          config:
            severity: warn
          arguments:
            combination_of_columns:
              - AUFNR
              - LOAD_DTS
    columns:
      - name: AUFNR
        tests:
          - not_null:
              config:
                severity: warn
      - name: LOAD_DTS
        tests:
          - not_null:
              config:
                severity: warn