version: 2
models:
  - name: v_psa_stg_bom_components__ml_ebs
    tests:
      - dbt_utils.unique_combination_of_columns:
          config:
            severity: warn
          arguments:
            combination_of_columns:
              - COMPONENT_SEQUENCE_ID
              - LOAD_DTS
    columns:
      - name: COMPONENT_SEQUENCE_ID
        tests:
          - not_null:
              config:
                severity: warn