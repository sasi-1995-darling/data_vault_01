version: 2
models:
  - name: v_psa_stg_bom_structures__ml_ebs
    tests:
      - dbt_utils.unique_combination_of_columns:
          config:
            severity: warn
          arguments:
            combination_of_columns:
              - BILL_SEQUENCE_ID
              - LOAD_DTS
    columns:
      - name: BOM_HK
        tests:
          - not_null:
              config:
                severity: warn