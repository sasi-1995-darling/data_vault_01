version: 2
models:
  - name: v_psa_stg_bom_permanent__winn_sap
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - BOM_HK
              - STLTY
              - LOAD_DTS
    columns:
      - name: BOM_BK
        tests:
          - not_null:
              config:
                severity: warn