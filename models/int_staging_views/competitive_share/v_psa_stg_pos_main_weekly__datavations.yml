version: 2
models:
- name: v_psa_stg_pos_main_weekly__datavations
  data_tests:
  - dbt_utils.unique_combination_of_columns:
      arguments:
        combination_of_columns:
        - COMPETITIVE_PRODUCT_BK
        - RETAILER_BK
        - REPORTING_PERIOD
        - REGION
        - STATE
        - LOWES_REGION
        - HOMEDEPOT_REGION
        - LOAD_DTS
      config:
        severity: warn
  columns:
  - name: REPORTING_PERIOD
    data_tests:
    - not_null:
        config:
          severity: warn

