version: 2
models:
- name: v_psa_stg_supplier_site__lrsn_psft
  data_tests:
  - dbt_utils.unique_combination_of_columns:
      arguments:
        combination_of_columns:
        - SUPPLIER_SITE_BK
        - VENDOR_ID
        - VNDR_LOC
        - SETID
        - EFFDT
        - LOAD_DTS
      config:
        severity: warn
  columns:
  - name: SUPPLIER_SITE_BK
    data_tests:
    - not_null:
        config:
          severity: warn
