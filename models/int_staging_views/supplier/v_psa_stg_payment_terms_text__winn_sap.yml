version: 2
models:
- name: v_psa_stg_payment_terms_text__winn_sap
  data_tests:
  - dbt_utils.unique_combination_of_columns:
      arguments:
        combination_of_columns:
        - ZTERM
        - SPRAS
        - ZTAGG
        - LOAD_DTS
      config:
        severity: warn
  columns: []
