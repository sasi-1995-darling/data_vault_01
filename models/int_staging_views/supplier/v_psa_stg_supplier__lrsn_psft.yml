version: 2
models:
  - name: v_psa_stg_supplier__lrsn_psft
    tests:
      - dbt_utils.unique_combination_of_columns:
          config:
            severity: warn
          arguments:
            combination_of_columns:
              - VENDOR_ID
              - LOAD_DTS
    columns:
      - name: VENDOR_ID
        tests:
          - not_null:
              config:
                severity: warn