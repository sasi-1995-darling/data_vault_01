version: 2
models:
  - name: v_psa_stg_external_claims
    tests:
      - dbt_utils.unique_combination_of_columns:
          config:
            severity: warn
          arguments:
            combination_of_columns:
              - CLAIM_NUMBER
              - _LINE
              - LOAD_DTS
    columns: []