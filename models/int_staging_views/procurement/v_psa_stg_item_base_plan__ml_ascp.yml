version: 2
models:
  - name: v_psa_stg_item_base_plan__ml_ascp
    tests:
      - dbt_utils.unique_combination_of_columns:
          config:
            severity: warn
          arguments:
            combination_of_columns:
              - ORGANIZATION_ID
              - INVENTORY_ITEM_ID
              - LOAD_DTS
    columns:
      - name: PLAN_ID
        tests:
          - not_null:
              config:
                severity: warn