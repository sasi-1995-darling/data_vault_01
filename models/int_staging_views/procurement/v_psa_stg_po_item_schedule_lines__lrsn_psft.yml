version: 2
models:
  - name: v_psa_stg_po_item_schedule_lines__lrsn_psft
    tests:
      - dbt_utils.unique_combination_of_columns:
          config:
            severity: warn
          arguments:
            combination_of_columns:
              - BUSINESS_UNIT
              - PO_ID
              - LINE_NBR
              - SCHED_NBR
              - LOAD_DTS
    columns:
      - name: BUSINESS_UNIT
        tests:
          - not_null:
              config:
                severity: warn
      - name: PO_ID
        tests:
          - not_null:
              config:
                severity: warn
      - name: LINE_NBR
        tests:
          - not_null:
              config:
                severity: warn