version: 2
models:
- name: v_psa_stg_vendor_retail_procurement_order__amazon
  data_tests:
  - dbt_utils.unique_combination_of_columns:
      arguments:
        combination_of_columns:
        - PURCHASE_ORDER_NUMBER
        - PURCHASE_ORDER_DATE
        - LOAD_DTS
      config:
        severity: warn
  columns: []
