version: 2
models:
  - name: v_psa_stg_po_item__ml_ebs
    tests:
      - dbt_utils.unique_combination_of_columns:
          config:
            severity: warn
          arguments:
            combination_of_columns:
              - PO_ITEM_HK
              - LOAD_DTS
    columns:
      - name: PO_HEADER_ID
        tests:
          - not_null:
              config:
                severity: warn
      - name: LINE_NUM
        tests:
          - not_null:
              config:
                severity: warn