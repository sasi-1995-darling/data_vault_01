version: 2
models:
  - name: v_psa_stg_planned_order__winn_sap
    tests:
      - dbt_utils.unique_combination_of_columns:
          config:
            severity: warn
          arguments:
            combination_of_columns:
              - PLNUM
              - LOAD_DTS
    columns: []