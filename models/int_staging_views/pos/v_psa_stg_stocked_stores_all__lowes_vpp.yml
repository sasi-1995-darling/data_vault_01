version: 2
models:
  - name: v_psa_stg_stocked_stores_all__lowes_vpp
    tests:
      - dbt_utils.unique_combination_of_columns:
          config:
            severity: warn
          arguments:
            combination_of_columns:
              - LOWES_SKU_BK
              - WEEK_ID
              - HOVBU_DESC
              - LOAD_DTS
    columns:
      - name: LOWES_SKU_BK
        tests:
          - not_null:
              config:
                severity: warn
      - name: WEEK_ID
        tests:
          - not_null:
              config:
                severity: warn
      - name: HOVBU_DESC
        tests:
          - not_null:
              config:
                severity: warn
      - name: LOAD_DTS
        tests:
          - not_null:
              config:
                severity: warn