version: 2
models:
  - name: v_psa_stg_sales_moen_inc__amazon_fivetran
    data_tests:
    - dbt_utils.unique_combination_of_columns:
        arguments:
          combination_of_columns:
          - REPORT_END_DATE
          - ASIN
          - VENDORCENTRAL_ACCOUNT
          - CUSTOMER_RETURNS
          - LOAD_DTS
        config:
          severity: warn
    columns: []
