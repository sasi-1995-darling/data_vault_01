version: 2
models:
- name: v_psa_stg_capacity_availability_interval__winn_sap
  data_tests:
  - dbt_utils.unique_combination_of_columns:
      arguments:
        combination_of_columns:
        - KAPID
        - VERSN
        - DATUB
        - LOAD_DTS
      config:
        severity: warn
  columns: []