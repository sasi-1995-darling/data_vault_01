version: 2
models:
- name: v_psa_stg_insurance_partner_name
  data_tests:
  - dbt_utils.unique_combination_of_columns:
      arguments:
        combination_of_columns:
        - PARTNER_NUMBER
        - INSURANCE_PARTNER
        - LOAD_DTS
      config:
        severity: warn
  columns:
  - name: PARTNER_NUMBER
    data_tests:
    - not_null:
        config:
          severity: warn
