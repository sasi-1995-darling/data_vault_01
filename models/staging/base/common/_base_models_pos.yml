version: 2

models:
  - name: base_pos_fiscal_calendar
    description: This is the base staging model for pos customer fiscal Calendar
    tests:
      - 'dbt_constraints.primary_key:':
          arguments:
            column_names:
              - datekey
    columns:
      - name: date
        tests:
          - not_null