version: 2

models:
  - name: stg_categories_b__ml_ebs
    description: This is the staging model for TMLC EBS Catergories
    tests:
      - 'dbt_constraints.primary_key:':
          arguments:
            column_names:
              - category_id
  - name: stg_category_sets_tl__ml_ebs
    description: This is the staging model for TMLC EBS Catergory Sets
    tests:
      - 'dbt_constraints.primary_key:':
          arguments:
            column_names:
              - category_set_id
  - name: stg_hr_all_organization_units__ml_ebs
    description: This is the staging model for TMLC EBS Plants
    tests:
      - 'dbt_constraints.primary_key:':
          arguments:
            column_names:
              - plant_id
  - name: stg_systems_items__ml_ebs
    description: This is the staging model for TMLC EBS Sytem Inventory
    tests:
      - 'dbt_constraints.primary_key:':
          arguments:
            column_names:
              - inventory_item_id
              - organization_id
  - name: stg_item_categories__ml_ebs
    description: This is the staging model for TMLC EBS Inventory Categories
    tests:
      - 'dbt_constraints.primary_key:':
          arguments:
            column_names:
              - inventory_item_id
              - organization_id
              - category_set_id
              - category_id
              - last_update_date
  - name: stg_cust_account__ml_ebs
    description: This is the staging model for TMLC EBS Shipment customer account
    tests:
      - 'dbt_constraints.primary_key:':
          arguments:
            column_names:
              - cust_account_id
  - name: stg_cust_acct_site__ml_ebs
    description: This is the staging model for TMLC EBS Shipment customer account site
    tests:
      - 'dbt_constraints.primary_key:':
          arguments:
            column_names:
              - cust_acct_site_id
  - name: stg_invoice_header__ml_ebs
    description: This is the staging model for TMLC EBS Shipment invoice headers transactions
    tests:
      - 'dbt_constraints.primary_key:':
          arguments:
            column_names:
              - customer_trx_id
  - name: stg_location__ml_ebs
    description: This is the staging model for TMLC EBS Shipment locations
    tests:
      - 'dbt_constraints.primary_key:':
          arguments:
            column_names:
              - location_id
  - name: stg_party__ml_ebs
    description: This is the staging model for TMLC EBS Shipment parties
    tests:
      - 'dbt_constraints.primary_key:':
          arguments:
            column_names:
              - party_id
  - name: stg_party_site__ml_ebs
    description: This is the staging model for TMLC EBS Shipment parties
    tests:
      - 'dbt_constraints.primary_key:':
          arguments:
            column_names:
              - party_site_id
  - name: stg_order_line__ml_ebs
    description: This is the staging model for TMLC EBS Shipment order line
    tests:
      - 'dbt_constraints.primary_key:':
          arguments:
            column_names:
              - line_id
  - name: stg_payment_schedule__ml_ebs
    description: This is the staging model for TMLC EBS Shipment payment schedule
    tests:
      - 'dbt_constraints.primary_key:':
          arguments:
            column_names:
              - customer_trx_id
  - name: stg_brand__ml_ebs
    description: This is the staging model for TMLC EBS Shipment payment schedule
    tests:
      - 'dbt_constraints.primary_key:':
          arguments:
            column_names:
              - brand_bk
    columns:
      - name: rec_src
        tests:
          - not_null