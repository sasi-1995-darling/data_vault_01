version: 2
models:
- name: lnk_same_as_key_account_group
  data_tests:
  - dbt_constraints.primary_key:
      arguments:
        column_names:
        - SLNK_KEY_ACCOUNT_GROUP_HK
  - dbt_expectations.expect_table_row_count_to_be_between:
      arguments:
        min_value: 4
      config:
        severity: warn
  columns:
  - name: KEY_ACCOUNT_GROUP_HK
    data_tests:
    - dbt_constraints.foreign_key:
        arguments:
          pk_table_name: ref( 'hub_key_account_group' )
          pk_column_name: KEY_ACCOUNT_GROUP_HK
  - name: SAME_AS_KEY_ACCOUNT_GROUP_HK
    data_tests:
    - dbt_constraints.foreign_key:
        arguments:
          pk_table_name: ref( 'hub_key_account_group' )
          pk_column_name: KEY_ACCOUNT_GROUP_HK
