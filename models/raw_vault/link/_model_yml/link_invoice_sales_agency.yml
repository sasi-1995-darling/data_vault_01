version: 2

models:
  - name: link_invoice_sales_agency
    description: This is the raw vault link model between invoice line and sales agency
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - invoice_sales_agency_hk
      - dbt_constraints.foreign_key:
          arguments:
            fk_column_names:
              - sales_agency_hk
            pk_table_name: ref('hub_sales_agency')
            pk_column_names:
              - sales_agency_hk
      - dbt_constraints.foreign_key:
          arguments:
            fk_column_names:
              - invoice_hk
            pk_table_name: ref('hub_invoice')
            pk_column_names:
              - invoice_hk
    config:
      tags:
        - emtek