version: 2
models:
  - name: lnk_asin_sns_categories
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - ASIN_SNS_CATEGORY_HK
      - dbt_expectations.expect_table_row_count_to_be_between:
          config:
            severity: warn
          arguments:
            min_value: 4
    columns: []