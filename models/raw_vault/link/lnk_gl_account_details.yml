version: 2
models:
  - name: lnk_gl_account_details
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - GL_ACCOUNT_DETAILS_HK
      - dbt_expectations.expect_table_row_count_to_be_between:
          config:
            severity: warn
          arguments:
            min_value: 4