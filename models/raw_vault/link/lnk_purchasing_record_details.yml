version: 2
models:
  - name: lnk_purchasing_record_details
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - PURCHASING_RECORD_DETAILS_HK
      - dbt_expectations.expect_table_row_count_to_be_between:
          config:
            severity: warn
          arguments:
            min_value: 4
    columns: []