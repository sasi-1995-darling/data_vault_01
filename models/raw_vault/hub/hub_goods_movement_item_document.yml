version: 2
models:
  - name: hub_goods_movement_item_document
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - GOODS_MOVEMENT_ITEM_DOCUMENT_HK
      - dbt_expectations.expect_table_row_count_to_be_between:
          config:
            severity: warn
          arguments:
            min_value: 4
    columns: []