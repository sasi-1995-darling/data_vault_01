version: 2
models:
  - name: hub_production_order_line
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - PRODUCTION_ORDER_LINE_HK
      - dbt_expectations.expect_table_row_count_to_be_between:
          config:
            severity: warn
          arguments:
            min_value: 4
    columns: []