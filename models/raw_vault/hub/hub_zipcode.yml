version: 2
models:
  - name: hub_zipcode
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - ZIP_HK
    columns:
      - name: ZIP_HK
        tests:
          - unique:
              config:
                severity: warn
          - not_null:
              config:
                severity: warn