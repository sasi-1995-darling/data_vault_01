version: 2
models:
  - name: hub_cost_value_type
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - COST_VALUE_TYPE_HK
    columns:
      - name: COST_VALUE_TYPE_HK
        tests:
          - unique:
              config:
                severity: warn
          - not_null:
              config:
                severity: warn