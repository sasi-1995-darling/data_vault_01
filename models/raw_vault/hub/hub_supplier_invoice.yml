version: 2
models:
- name: hub_supplier_invoice
  data_tests:
  - dbt_constraints.primary_key:
      name: pk_hub_supplier_invoice
      arguments:
        column_names:
        - SUPPLIER_INVOICE_HK
      config:
        severity: warn
  - dbt_expectations.expect_table_row_count_to_be_between:
      name: rowcount_hub_supplier_invoice
      arguments:
        min_value: 4
      config:
        severity: warn
  columns: []
  config:
    tags:
    - hub
