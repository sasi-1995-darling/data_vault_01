version: 2

models:
  - name: hub_customer_account
    description: This is the raw vault hub model for customer accounts
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - account_hk
    config:
      tags:
        - emtek
        - mlc_ebs
        - tt_e21