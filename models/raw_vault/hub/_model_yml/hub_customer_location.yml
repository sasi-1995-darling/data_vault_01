version: 2

models:
  - name: hub_customer_location
    description: This is the raw vault hub model for pos customer location
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - customer_location_hk
    config:
      tags:
        - emtek