version: 2

models:
  - name: hub_sales_territory
    description: This is the raw vault hub model for sales territories
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - sales_territory_hk
    config:
      tags:
        - emtek