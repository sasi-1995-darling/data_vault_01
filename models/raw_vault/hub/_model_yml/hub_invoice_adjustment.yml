version: 2

models:
  - name: hub_invoice_adjustment
    description: This is the raw vault hub model for invoice adjustments
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - invoice_adjustment_hk
    config:
      tags:
        - emtek