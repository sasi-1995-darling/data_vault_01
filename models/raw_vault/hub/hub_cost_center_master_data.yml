version: 2
models:
  - name: hub_cost_center_master_data
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - COST_CENTER_MASTER_DATA_HK
      - dbt_expectations.expect_table_row_count_to_be_between:
          config:
            severity: warn
          arguments:
            min_value: 4
    columns: []