version: 2
models:
- name: hub_location_assignment
  data_tests:
  - dbt_constraints.primary_key:
      arguments:
        column_names:
        - LOCATION_ASSIGNMENT_HK
      config:
        severity: warn
  - dbt_expectations.expect_table_row_count_to_be_between:
      arguments:
        min_value: 4
      config:
        severity: warn
  columns: []
  config:
    tags:
      - hub
