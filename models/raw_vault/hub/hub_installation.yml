version: 2
models:
- name: hub_installation
  data_tests:
  - dbt_expectations.expect_table_row_count_to_be_between:
      arguments:
        min_value: 4
      config:
        severity: warn
  columns:
  - name: INSTALLATION_HK
    data_tests:
    - unique:
        config:
          severity: warn
