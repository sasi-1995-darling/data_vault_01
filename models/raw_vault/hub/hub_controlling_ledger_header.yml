version: 2
models:
  - name: hub_controlling_ledger_header
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - CONTROLLING_LEDGER_HEADER_HK
      - dbt_expectations.expect_table_row_count_to_be_between:
          config:
            severity: warn
          arguments:
            min_value: 4