version: 2
models:
  - name: hub_gl_header
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - GENERAL_LEDGER_HK
      - dbt_expectations.expect_table_row_count_to_be_between:
          config:
            severity: warn
          arguments:
            min_value: 4