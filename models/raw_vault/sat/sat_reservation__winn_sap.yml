version: 2
models:
  - name: sat_reservation__winn_sap
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - RESERVATION_HK
              - LOAD_DTS
      - dbt_expectations.expect_table_row_count_to_be_between:
          config:
            severity: warn
          arguments:
            min_value: 4
    columns:
      - name: RESERVATION_HK
        tests:
          - not_null:
              config:
                severity: warn
          - dbt_constraints.foreign_key:
              arguments:
                pk_table_name: ref( 'hub_reservation' )
                pk_column_name: RESERVATION_HK