version: 2

models:
  - name: sat_invoice_detail__emtk_ebs
    description: This is the raw vault satellite model for invoice detail
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - invoice_hk
              - load_dts
      - dbt_constraints.foreign_key:
          arguments:
            fk_column_names:
              - invoice_hk
            pk_table_name: ref('hub_invoice')
            pk_column_names:
              - invoice_hk
    config:
      tags:
        - emtek
        - ebs