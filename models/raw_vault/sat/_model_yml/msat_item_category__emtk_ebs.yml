version: 2

models:
  - name: msat_item_category__emtk_ebs
    description: This is the raw vault multi-active satellite model for item category
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - item_hk
              - category_id
              - category_set_id
              - load_dts
      - dbt_constraints.foreign_key:
          arguments:
            fk_column_names:
              - item_hk
            pk_table_name: ref('hub_item')
            pk_column_names:
              - item_hk
    config:
      tags:
        - emtek
        - ebs