version: 2

models:
  - name: sat_contact_detail__emtk_ebs
    description: This is the raw vault satellite model for contact detail
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - contact_hk
              - load_dts
      - dbt_constraints.foreign_key:
          arguments:
            fk_column_names:
              - contact_hk
            pk_table_name: ref('hub_contact')
            pk_column_names:
              - contact_hk
    config:
      tags:
        - emtek
        - ebs