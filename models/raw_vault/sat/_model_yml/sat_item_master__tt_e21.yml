version: 2

models:
  - name: sat_item_master__tt_e21
    description: This is the raw vault satellite model for thermatru product master
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - item_hk
              - load_dts
      - dbt_constraints.foreign_key:
          arguments:
            fk_column_names:
              - item_hk
            pk_table_name: ref('hub_item')
            pk_column_names:
              - item_hk