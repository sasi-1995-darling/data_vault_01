version: 2

models:
  - name: sat_pos_product_customer__ferguson_extract
    description: This is the raw vault satellite model for pos product customer for ferguson
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - product_customer_hk
              - load_dts
      - dbt_constraints.foreign_key:
          arguments:
            fk_column_names:
              - sell_location_id_hk
            pk_table_name: ref('hub_customer_location')
            pk_column_names:
              - customer_location_hk