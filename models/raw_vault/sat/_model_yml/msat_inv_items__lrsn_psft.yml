version: 2

models:
  - name: msat_inv_items__lrsn_psft
    description: This is the raw vault multi-active satellite model for Larson product master
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - item_hk
              - effdt
              - load_dts
      - dbt_constraints.foreign_key:
          arguments:
            fk_column_names:
              - item_hk
            pk_table_name: ref('hub_item')
            pk_column_names:
              - item_hk