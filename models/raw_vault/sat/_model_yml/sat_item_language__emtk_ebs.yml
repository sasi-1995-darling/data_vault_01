version: 2

models:
  - name: sat_item_language__emtk_ebs
    description: This is the raw vault satellite model for item language (attributes that are language dependent)
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - item_hk
              - load_dts
      - dbt_constraints.foreign_key:
          arguments:
            fk_column_names:
              - item_hk
            pk_table_name: ref('hub_item')
            pk_column_names:
              - item_hk
    config:
      tags:
        - emtek
        - ebs