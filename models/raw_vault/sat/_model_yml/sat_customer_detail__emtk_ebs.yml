version: 2

models:
  - name: sat_customer_detail__emtk_ebs
    description: This is the raw vault satellite model for customer detail
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - customer_hk
              - load_dts
      - dbt_constraints.foreign_key:
          arguments:
            fk_column_names:
              - customer_hk
            pk_table_name: ref('hub_customer')
            pk_column_names:
              - customer_hk
    config:
      tags:
        - emtek
        - ebs