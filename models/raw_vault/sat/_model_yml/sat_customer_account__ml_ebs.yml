version: 2

models:
  - name: sat_customer_account__ml_ebs
    description: This is the raw vault satellite model for customer for ml ebs
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - account_hk
              - load_dts
      - dbt_constraints.foreign_key:
          arguments:
            fk_column_names:
              - account_hk
            pk_table_name: ref('hub_customer_account')
            pk_column_names:
              - account_hk