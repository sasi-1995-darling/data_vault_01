version: 2

models:
  - name: sat_invoice_adjustment_detail__emtk_ebs
    description: This is the raw vault satellite model for invoice adjustment detail
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - invoice_adjustment_hk
              - load_dts
      - dbt_constraints.foreign_key:
          arguments:
            fk_column_names:
              - invoice_adjustment_hk
            pk_table_name: ref('hub_invoice_adjustment')
            pk_column_names:
              - invoice_adjustment_hk
    config:
      tags:
        - emtek
        - ebs