version: 2

models:
  - name: sat_po_line_shipment_detail__emtk_ebs
    description: This is the raw vault satellite model for po line shipment detail
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - po_line_shipment_hk
              - load_dts
      - dbt_constraints.foreign_key:
          arguments:
            fk_column_names:
              - po_line_shipment_hk
            pk_table_name: ref('hub_po_line_shipment')
            pk_column_names:
              - po_line_shipment_hk
    config:
      tags:
        - emtek
        - ebs