version: 2

models:
  - name: sat_po_detail__emtk_ebs
    description: This is the raw vault satellite model for purchase order detail
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - purchase_order_hk
              - load_dts
      - dbt_constraints.foreign_key:
          arguments:
            fk_column_names:
              - purchase_order_hk
            pk_table_name: ref('hub_purchase_order')
            pk_column_names:
              - purchase_order_hk
    config:
      tags:
        - emtek
        - ebs