---- SRC LAYER ----
WITH
SRC_s              as ( SELECT CHANNEL_SOLD_ITEM_LHK, ASIN, CUSTOMER_RETURNS, END_DATE, HASHDIFF, LOAD_DTS, MARKETPLACE_ID, ORDERED_REVENUE_AMOUNT, ORDERED_REVENUE_CURRENCY_CODE, ORDERED_UNITS, PSA_DELETE_IND, PSA_LOAD_DTS, PSA_RECORD_SOURCE, REC_SRC, SHIPPED_COGS_AMOUNT, SHIPPED_COGS_CURRENCY_CODE, SHIPPED_REVENUE_AMOUNT, SHIPPED_REVENUE_CURRENCY_CODE, SHIPPED_UNITS, START_DATE, _FIVETRAN_SYNCED FROM {{ ref('v_psa_stg_channel_sold_items__amazon_moen_inc') }} as SRC 
                        {% if is_incremental() %}
                        WHERE SRC.LOAD_DTS > (SELECT DATEADD('HOUR', '-1', MAX(LOAD_DTS)) FROM {{this}}){% endif %} )

/*
SRC_s              as ( SELECT * FROM STAGING.v_psa_stg_channel_sold_items__amazon_moen_inc )
*/
---- LOGIC LAYER ----

, LOGIC_s as (
    SELECT
        ASIN
      , END_DATE
      , MARKETPLACE_ID
      , START_DATE
      , CUSTOMER_RETURNS
      , ORDERED_REVENUE_AMOUNT
      , ORDERED_REVENUE_CURRENCY_CODE
      , ORDERED_UNITS
      , SHIPPED_COGS_AMOUNT
      , SHIPPED_COGS_CURRENCY_CODE
      , SHIPPED_REVENUE_AMOUNT
      , SHIPPED_REVENUE_CURRENCY_CODE
      , SHIPPED_UNITS
      , _FIVETRAN_SYNCED
      , PSA_LOAD_DTS
      , PSA_RECORD_SOURCE
      , PSA_DELETE_IND
      , LOAD_DTS
      , REC_SRC
      , CHANNEL_SOLD_ITEM_LHK
      , HASHDIFF
    FROM SRC_s
)
---- RENAME LAYER ----

, RENAME_s as (
    SELECT
        ASIN
      , END_DATE
      , MARKETPLACE_ID
      , START_DATE
      , CUSTOMER_RETURNS
      , ORDERED_REVENUE_AMOUNT
      , ORDERED_REVENUE_CURRENCY_CODE
      , ORDERED_UNITS
      , SHIPPED_COGS_AMOUNT
      , SHIPPED_COGS_CURRENCY_CODE
      , SHIPPED_REVENUE_AMOUNT
      , SHIPPED_REVENUE_CURRENCY_CODE
      , SHIPPED_UNITS
      , _FIVETRAN_SYNCED
      , PSA_LOAD_DTS
      , PSA_RECORD_SOURCE
      , PSA_DELETE_IND
      , LOAD_DTS
      , REC_SRC
      , CHANNEL_SOLD_ITEM_LHK
      , HASHDIFF
    FROM LOGIC_s
)
---- FILTER LAYER ----

, FILTER_s as (
    SELECT *
    FROM RENAME_s
)

---- JOIN LAYER ----
, JOIN_RESULT as (
    SELECT *
    FROM FILTER_s
)

---- FINAL LAYER ----
SELECT
          ASIN
        , END_DATE
        , MARKETPLACE_ID
        , START_DATE
        , CUSTOMER_RETURNS
        , ORDERED_REVENUE_AMOUNT
        , ORDERED_REVENUE_CURRENCY_CODE
        , ORDERED_UNITS
        , SHIPPED_COGS_AMOUNT
        , SHIPPED_COGS_CURRENCY_CODE
        , SHIPPED_REVENUE_AMOUNT
        , SHIPPED_REVENUE_CURRENCY_CODE
        , SHIPPED_UNITS
        , _FIVETRAN_SYNCED
        , PSA_LOAD_DTS
        , PSA_RECORD_SOURCE
        , PSA_DELETE_IND
        , CHANNEL_SOLD_ITEM_LHK
        , LOAD_DTS
        , REC_SRC
        , HASHDIFF
FROM JOIN_RESULT
{% if is_incremental() %}
WHERE NOT EXISTS (
SELECT 1 
FROM {{ this }} existing
WHERE existing.CHANNEL_SOLD_ITEM_LHK = JOIN_RESULT.CHANNEL_SOLD_ITEM_LHK AND existing.HASHDIFF = JOIN_RESULT.HASHDIFF
)
{% endif %}
{% if not is_incremental() %}

/* The following qualifier is implemented to prevent multiple loads of touched records during the initial build, such as multiple rows per HK and hashdiff. */
qualify 1 = row_number() over (partition by CHANNEL_SOLD_ITEM_LHK, HASHDIFF order by PSA_LOAD_DTS)

union all
SELECT 
GR.VALUE::text AS ASIN,
GR.VALUE::DATE AS END_DATE,
GR.VALUE::text AS MARKETPLACE_ID,
GR.VALUE::DATE AS START_DATE,
NULL AS CUSTOMER_RETURNS,
NULL AS ORDERED_REVENUE_AMOUNT,
NULL AS ORDERED_REVENUE_CURRENCY_CODE,
NULL AS ORDERED_UNITS,
NULL AS SHIPPED_COGS_AMOUNT,
NULL AS SHIPPED_COGS_CURRENCY_CODE,
NULL AS SHIPPED_REVENUE_AMOUNT,
NULL AS SHIPPED_REVENUE_CURRENCY_CODE,
NULL AS SHIPPED_UNITS,
NULL AS _FIVETRAN_SYNCED,
NULL AS PSA_LOAD_DTS,
NULL AS PSA_RECORD_SOURCE,
NULL AS PSA_DELETE_IND,
MD5_BINARY(GR.VALUE) AS CHANNEL_SOLD_ITEM_LHK,
CONVERT_TIMEZONE('UTC','1900-01-01'::TIMESTAMP) AS LOAD_DTS,
'USAZET.SNOWFLAKE.FBIN.DERIVED' AS REC_SRC,
''::BINARY AS HASHDIFF
FROM
TABLE(strtok_split_to_table('0|-1|-2', '|')) AS GR
{% endif %}