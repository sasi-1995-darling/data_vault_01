version: 2
models:
- name: sat_brand__profitero_share
  data_tests:
  - dbt_constraints.primary_key:
      arguments:
        column_names:
        - BRAND_HK
        - DIM_BRAND_KEY
        - LOAD_DTS
  - dbt_expectations.expect_table_row_count_to_be_between:
      arguments:
        min_value: 4
      config:
        severity: warn
  columns: []
