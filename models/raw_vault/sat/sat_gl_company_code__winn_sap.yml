version: 2
models:
  - name: sat_gl_company_code__winn_sap
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - GL_ACCOUNT_DETAILS_HK
              - LOAD_DTS
      - dbt_expectations.expect_table_row_count_to_be_between:
          config:
            severity: warn
          arguments:
            min_value: 4
    columns:
      - name: GL_ACCOUNT_DETAILS_HK
        tests:
          - not_null:
              config:
                severity: warn