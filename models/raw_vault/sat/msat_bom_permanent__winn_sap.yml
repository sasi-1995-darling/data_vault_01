version: 2
models:
  - name: msat_bom_permanent__winn_sap
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - BOM_HK
              - STLTY
              - LOAD_DTS
      - dbt_expectations.expect_table_row_count_to_be_between:
          config:
            severity: warn
          arguments:
            min_value: 4