version: 2
models:
  - name: sat_planned_order__ml_ascp
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - PLANNED_ORDER_HK
              - LOAD_DTS
      - dbt_expectations.expect_table_row_count_to_be_between:
          config:
            severity: warn
          arguments:
            min_value: 4
    columns:
      - name: PLANNED_ORDER_HK
        tests:
          - dbt_constraints.foreign_key:
              arguments:
                pk_table_name: ref( 'hub_planned_order' )
                pk_column_name: PLANNED_ORDER_HK
      - name: BKCC
        tests:
          - not_null
      - name: REC_SRC
        tests:
          - not_null
      - name: LOAD_DTS
        tests:
          - not_null