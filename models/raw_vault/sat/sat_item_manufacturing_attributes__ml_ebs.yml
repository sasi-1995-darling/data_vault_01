version: 2
models:
  - name: sat_item_manufacturing_attributes__ml_ebs
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - INVENTORY_ITEM_ID
              - LOAD_DTS
      - dbt_expectations.expect_table_row_count_to_be_between:
          config:
            severity: warn
          arguments:
            min_value: 4
    columns:
      - name: ITEM_HK
        tests:
          - not_null:
              config:
                severity: warn
          - dbt_constraints.foreign_key:
              arguments:
                pk_table_name: ref( 'hub_item_v1' )
                pk_column_name: ITEM_HK