version: 2
models:
- name: ref_capacity_category__winn_sap
  data_tests:
  - dbt_constraints.primary_key:
      arguments:
        column_names:
        - CAPACITY_CATEGORY
        - LANGUAGE_KEY
        - LOAD_DTS
  columns: []
