version: 2
models:
  - name: ref_hub_item_cat
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - ITEM_CAT_BK
    columns:
      - name: ITEM_CAT_BK
        tests:
          - unique:
              config:
                severity: warn
          - not_null:
              config:
                severity: warn