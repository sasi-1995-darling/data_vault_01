version: 2
models:
  - name: ref_item_subgroup_2__tt_e21_v1
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - PART_SUBGRP2_ID
    columns:
      - name: PART_SUBGRP2_ID
        tests:
          - unique:
              config:
                severity: warn
          - not_null:
              config:
                severity: warn
      - name: LOAD_DTS
        tests:
          - not_null:
              config:
                severity: warn