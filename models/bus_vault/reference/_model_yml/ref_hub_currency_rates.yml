version: 2
models:
  - name: ref_hub_currency_rates
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - CURR_RATES_BK
    columns:
      - name: CURR_RATES_BK
        tests:
          - unique:
              config:
                severity: warn
          - not_null:
              config:
                severity: warn