version: 2
models:
  - name: ref_hub_memo_line
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - MEMO_LINE_BK
    columns:
      - name: MEMO_LINE_BK
        tests:
          - unique:
              config:
                severity: warn
          - not_null:
              config:
                severity: warn