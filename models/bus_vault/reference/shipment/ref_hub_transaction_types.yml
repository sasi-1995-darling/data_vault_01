version: 2
models:
  - name: ref_hub_transaction_types
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - TRANSACTION_TYPE_BK
    columns:
      - name: TRANSACTION_TYPE_BK
        tests:
          - unique:
              config:
                severity: warn
          - not_null:
              config:
                severity: warn