version: 2
models:
- name: pb_dtc_order_line
  data_tests:
  - dbt_utils.unique_combination_of_columns:
      arguments:
        combination_of_columns:
        - ORDER_LINE_KEY
        - ADJUSTMENT_ID
        - REFUND_LINE_RECORD_ID
      config:
        severity: warn
  columns: []
