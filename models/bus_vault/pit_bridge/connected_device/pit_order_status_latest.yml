version: 2
models:
- name: pit_order_status_latest
  data_tests:
  - dbt_utils.unique_combination_of_columns:
      arguments:
        combination_of_columns:
        - SEQ_ID
      config:
        severity: warn
  columns: []
