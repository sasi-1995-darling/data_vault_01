version: 2
models:
  - name: fact_quality_issue
    columns:
      - name: ISSUE_NOTIFICATION_BK
        tests:
          - unique:
              config:
                severity: warn
          - not_null:
              config:
                severity: warn