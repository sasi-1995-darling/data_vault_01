version: 2
models:
  - name: fact_quality_causes
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - NOTIFICATION_ISSUE_CAUSE_BK
    columns:
      - name: NOTIFICATION_ISSUE_CAUSE_BK
        tests:
          - not_null:
              config:
                severity: warn