version: 2
models:
- name: fact_flow_events_device_period
  data_tests:
  - dbt_constraints.primary_key:
      arguments:
        column_names:
        - DEVICE_ID
        - PERIOD_TYPE
        - PERIOD_START_DATE_KEY
  columns: []
