version: 2
models:
  - name: fact_insurance_partner_order_farmers
    description: "Farmers-specific certified fact wrapper over pb_insurance_partner_order_farmers for legacy Farmers share parity."
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          config:
             severity: warn
          arguments:
            combination_of_columns:
              - ORDER_ID
              - SKU
              - ORDER_LINE_ID
              - ORIG_ORDER_TAG
    columns: []
