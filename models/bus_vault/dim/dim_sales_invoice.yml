version: 2
models:
  - name: dim_sales_invoice
    columns:
      - name: SALES_INVOICE_BK
        tests:
          - unique:
              config:
                severity: warn
          - not_null:
              config:
                severity: warn