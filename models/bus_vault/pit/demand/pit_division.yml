version: 2
models:
  - name: pit_division
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - DIVISION_HK
              - LANGUAGE_KEY
    columns: []