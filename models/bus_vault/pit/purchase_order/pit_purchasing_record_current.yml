version: 2
models:
  - name: pit_purchasing_record_current
    tests:
      - dbt_constraints.primary_key:
          arguments:
            column_names:
              - PURCHASING_RECORD_HK
    columns: []