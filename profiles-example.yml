default:
  target: default
  outputs:
    default:
      account: fbhs_gpg.east-us-2.azure
      database: DV_RAW_VAULT      # You shouldn't change this - DATAVAULT_DEV, DV_RAW_VAULT
      role: DV_RAW_VAULT_RW       # You shouldn't change this - DATA_ENGINEER, DV_RAW_VAULT_RW
      schema: DBT_MTRAN       # Change USERNAME to your actual username in snowflake. This is your playground
      threads: 1
      type: snowflake
      user: max.tran@fbwinn.com  # We are using Okta, so user should match to your fbin email address
      warehouse: PLAYGROUND_WH # DATA_ENGINEER_WH, PLAYGROUND_WH
      query_tag: 'dbt'
      authenticator: externalbrowser
